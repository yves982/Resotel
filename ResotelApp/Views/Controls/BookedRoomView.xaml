<UserControl x:Class="ResotelApp.Views.Controls.BookedRoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ResotelApp.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel VerticalAlignment="Top" Height="24" Orientation="Horizontal">
        <TextBlock TextAlignment="Center" Width="30" Text="{Binding Id}"></TextBlock>
        <Image Source="{Binding Room.RoomKind, Converter={StaticResource roomKindToImagePathConverter}}" Width="24"></Image>
        <TextBlock TextAlignment="Center" Width="40" Text="{Binding Booking.Dates.Days}"></TextBlock>
        <TextBlock TextAlignment="Center" Width="24" Text="{Binding Room.Price}"></TextBlock>
        
    </StackPanel>
</UserControl>
