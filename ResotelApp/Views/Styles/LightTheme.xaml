<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ResotelApp.Views.Styles">
    <Style TargetType="{x:Type Button}" x:Key="NavButton">
        <Setter Property="MinWidth" Value="70" />
        <Setter Property="Margin" Value="0,0,10,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border CornerRadius="4" Background="#F2F2F2" BorderThickness="1" Padding="2" BorderBrush="LightBlue">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="{x:Type HeaderedContentControl}">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border CornerRadius="3" BorderBrush="LightSteelBlue" BorderThickness="1" Margin="2,4,2,0">
                        <TextBlock HorizontalAlignment="Stretch" TextAlignment="Center" Text="{Binding}" Background="#F2F2F2" />
                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>